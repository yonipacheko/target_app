<%= form_for(@todo_client) do |f| %>
  <% if @todo_client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todo_client.errors.count, "error") %> prohibited this todo_client from being saved:</h2>

      <ul>
      <% @todo_client.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
